{
  "name": "services",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build:cdk": "pnpm --filter cdk build",
    "build:lambda": "pnpm --filter lambda build",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:all": "jest --config jest.config.ts",
    "deploy": "pnpm --filter cdk run cdk deploy",
    "synth": "pnpm --filter cdk run cdk synth"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^18.11.18",
    "aws-cdk": "^2.1016.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}